import { useState, useEffect } from 'react';

type VocabTermsProps = {
  // categories: string[];
  // setWords: React.Dispatch<React.SetStateAction<WordData[]>>;
}

const VocabTerms = ({  }: VocabTermsProps) => {

  return (
    <h1>Notecards</h1>
  )
}
  
export default VocabTerms;