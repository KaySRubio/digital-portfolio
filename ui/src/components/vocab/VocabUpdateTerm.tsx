import { useState, useEffect } from 'react';

type VocabUpdateTermProps = {
  // categories: string[];
  // setWords: React.Dispatch<React.SetStateAction<WordData[]>>;
}

const VocabUpdateTerm = ({  }: VocabUpdateTermProps) => {

  return (
    <h1>Notecards</h1>
  )
}
  
export default VocabUpdateTerm;